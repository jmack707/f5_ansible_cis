apiVersion: v1
kind: Service
metadata:
  name: {{ svc.name }}-svc
  namespace: {{ namespace }}
spec:
  selector:
    app: {{ svc.name }}
  ports:
    - protocol: TCP
      port: {{ svc.port }}
      targetPort: {{ svc.targetPort }}
